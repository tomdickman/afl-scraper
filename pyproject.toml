[project]
name = "afl-scraper"
version = "0.1.0"
description = "Data scraper for AFL matches and player statistics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.2.1",
    "playwright>=1.52.0",
]
