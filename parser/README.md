# AFL Scraper - Parser

This module contains the logic for extrating data from within fetched pages using playwright.

All fixed CSS selectors required to parse data from a page should be stored in here as the single module responsible for staying current with the upstream site structures if they change over time.
