# Models

Structured model definitions, the output of data transformations and used to define storage schemas.

These concrete models define what data looks like after the full pipeline cycle of, scraping -> normalisation/parsing -> transformation.

Data in these models is ready to store in the database.
