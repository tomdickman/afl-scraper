# AFL Scraper

A set of CLI scripts written in `python` to scrape AFL stats from various sources.

## Tooling

- `uv` for dependency package management
- `playwright` for browser automation and scraping JS heavy sites
